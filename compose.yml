services:
  review:
    image: team-d-kuroudo-engineer-review:5.9
    build:
      context: .
    volumes:
      - .:/book
    environment:
      - REVIEW_CONFIG_FILE=config.yml
    command: /bin/bash -lc "./setup.sh && npm run pdf"
